# Core dependencies
gspread==6.1.0
google-auth==2.27.0
google-auth-oauthlib==1.2.0
python-dotenv==1.0.0
anthropic==0.40.0
beautifulsoup4==4.12.3

# Firecrawl - primary scraping engine (v10.0)
firecrawl-py>=1.0.0

# Playwright - fallback scraping engine (used when Firecrawl unavailable)
playwright==1.40.0
playwright-stealth==1.0.6

# Experimental dependencies
capsolver>=1.0.0
curl_cffi>=0.7.0
