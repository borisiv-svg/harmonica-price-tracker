# Core dependencies
gspread==6.2.1
google-auth==2.49.1
google-auth-oauthlib==1.3.1
python-dotenv==1.2.2
anthropic==0.87.0
beautifulsoup4==4.14.3

# Primary scraping engine (async, parallel)
crawl4ai==0.8.6

# TLS impersonation for Cloudflare bypass
curl_cffi==0.14.0

# Firecrawl — JS rendering for DM, Randi, Glovo
# 1.17.0 is last 1.x release. Code uses FirecrawlApp + .scrape_url()
# which are 1.x API. 2.x+ has breaking changes.
firecrawl-py==1.17.0

# Anti-bot (optional)
capsolver==1.0.7

# Testing
pytest==9.0.2
